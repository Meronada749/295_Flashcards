{{ csrfField() }}
<div class="field">
  @!component('components/input', { name: 'name', label: 'Name', value: old('name') || (deck ? deck.name : '') })
</div>

<div class="field">
  @!component('components/input', { name: 'description', label: 'Description', value: old('description') || (deck ? deck.description : '') })
</div>

<button type="submit">Enregistrer</button>